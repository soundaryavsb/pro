#include <stdio.h>

int main(void) {
	char let;
	scanf("%c",&let);
	if(let=='a'||let=='e'||let=='i'||let=='o'||let=='u'||let=='A'||let=='E'||let=='I'||let=='O'||let=='U')
	{
		printf("vowel");
	}
	else
	{
		printf("consonant");
	}
	
	return 0;
}
